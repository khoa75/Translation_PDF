# Data Directory Structure

This directory contains all data-related files for the translation project:

- `raw/` - Raw datasets before preprocessing
- `processed/` - Preprocessed datasets ready for training
- `vocab/` - Vocabulary files for tokenization